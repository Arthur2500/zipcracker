# zipcracker - a CPU-only ZIP encryption brute force tool
 
Supports aes256 and zipcrypto encryption methods

### Usage
```
./zipcracker -f <file> [-l <password-length>] [-w <wordlist>] [-t <thread-count>] [-r] [-c <charset>]
```
